{
  "name": "seb-backend",
  "type": "module",
  "scripts": {
    "local": "wrangler dev --port 9999",
    "test:worker": "node scripts/reset-e2e-db.mjs && wrangler dev --port 9899 --var FRONTEND_ORIGINS:http://localhost:9880 --var ROLE_INVITE_SECRET:end-to-end-invite-secret-at-least-32-bytes --var PORTAL_BASE_URL:http://localhost:9880 --persist-to dev-web/.playwright/state 2>&1 | tee dev-web/.playwright/worker.log",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "fallow": "fallow --type-aware",
    "check": "npm run typecheck && npm run test:coverage && npm run fallow && npm run check:sdl && npm run db:schema:check",
    "db:schema:generate": "node scripts/check-schema.mjs --write",
    "db:schema:check": "node scripts/check-schema.mjs && node --no-warnings scripts/check-migrations.mjs",
    "db:setup:local": "wrangler d1 execute DB --local --file=database/schema.sql && node scripts/migrate.mjs --stamp",
    "db:migrate": "node scripts/migrate.mjs",
    "db:migrate:remote": "node scripts/migrate.mjs --remote",
    "seed:super-admin": "node scripts/seed-super-admin.mjs",
    "check:sdl": "node scripts/check-sdl-descriptions.mjs"
  },
  "dependencies": {
    "@noble/hashes": "^2.3.0",
    "aws4fetch": "^1.0.20",
    "dataloader": "^2.2.3",
    "drizzle-orm": "^0.45.2",
    "graphql": "^17.0.2",
    "graphql-yoga": "^5.22.0",
    "hono": "^4.13.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "@types/node": "^26.2.0",
    "@vitest/coverage-istanbul": "^3.2.7",
    "drizzle-kit": "^0.31.10",
    "fallow": "^3.17.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7",
    "wrangler": "^4.110.0"
  }
}
