query CurrentSession {
  auth {
    currentSession {
      success
      message
      response {
        user { id email emailVerified displayName roles capabilities createdAt }
        session { id current createdAt expiresAt }
      }
    }
  }
}
